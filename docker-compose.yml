services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: staybook
      POSTGRES_PASSWORD: staybook
      POSTGRES_DB: staybook
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
